builds:
  - doot_release:
    - sources: 'main.cpp, util.cpp, parser.cpp'
    - libraries: ncurses

targets:
  - doot:
    - builds:
      - default: doot_release
    - postbuilds:
      - default: '{}'
